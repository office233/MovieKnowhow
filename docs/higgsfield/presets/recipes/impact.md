# Impact (`/impact`)

- **output:** video
- **description:** A burst of particles frozen around the product.

Resolve with `get_preset_instructions(preset:'/impact')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
