# Moodboard (`/moodboard`)

- **output:** image
- **description:** A branded moodboard built around the product.

Resolve with `get_preset_instructions(preset:'/moodboard')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
