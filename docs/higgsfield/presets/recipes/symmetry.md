# Symmetry (`/symmetry`)

- **output:** image
- **description:** A perfectly symmetrical composition.

Resolve with `get_preset_instructions(preset:'/symmetry')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
