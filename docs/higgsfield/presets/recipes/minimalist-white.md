# Minimalist White (`/minimalist-white`)

- **output:** image
- **description:** A clean product image for a store.

Resolve with `get_preset_instructions(preset:'/minimalist-white')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
