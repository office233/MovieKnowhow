# Retail Stack (`/retail-stack`)

- **output:** image
- **description:** Multiple product packages grouped together.

Resolve with `get_preset_instructions(preset:'/retail-stack')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
