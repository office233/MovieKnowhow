# Unboxing (`/unboxing`)

- **output:** image
- **description:** An open box revealing its contents.

Resolve with `get_preset_instructions(preset:'/unboxing')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
