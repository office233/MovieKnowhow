# Signature Frame (`/signature-frame`)

- **output:** image
- **description:** A signature brand visual.

Resolve with `get_preset_instructions(preset:'/signature-frame')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
