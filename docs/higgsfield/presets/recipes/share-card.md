# Share Card (`/share-card`)

- **output:** image
- **description:** A visual designed for sharing.

Resolve with `get_preset_instructions(preset:'/share-card')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
