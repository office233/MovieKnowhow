# Saveable Tip (`/saveable-tip`)

- **output:** image
- **description:** A useful tip card designed to be saved.

Resolve with `get_preset_instructions(preset:'/saveable-tip')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
