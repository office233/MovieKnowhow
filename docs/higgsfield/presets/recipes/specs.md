# Specs (`/specs`)

- **output:** image
- **description:** The product's key specifications.

Resolve with `get_preset_instructions(preset:'/specs')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
