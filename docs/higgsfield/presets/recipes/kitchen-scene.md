# Kitchen Scene (`/kitchen-scene`)

- **output:** image
- **description:** The product in a kitchen setting.

Resolve with `get_preset_instructions(preset:'/kitchen-scene')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
