# Before After (`/before-after`)

- **output:** image
- **description:** The product as a solution to a specific problem.

Resolve with `get_preset_instructions(preset:'/before-after')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
