# Hero Shot (`/hero-shot`)

- **output:** image
- **description:** The product as the hero of the frame.

Resolve with `get_preset_instructions(preset:'/hero-shot')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
