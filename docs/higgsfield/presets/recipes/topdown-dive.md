# Topdown Dive (`/topdown-dive`)

- **output:** video
- **description:** A transition from an overhead view to a close-up.

Resolve with `get_preset_instructions(preset:'/topdown-dive')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
