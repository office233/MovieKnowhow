# Shelf Ready (`/shelf-ready`)

- **output:** image
- **description:** A product presentation on a shelf.

Resolve with `get_preset_instructions(preset:'/shelf-ready')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
