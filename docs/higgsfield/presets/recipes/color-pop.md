# Color Pop (`/color-pop`)

- **output:** image
- **description:** Bold colorful shapes surrounding the product.

Resolve with `get_preset_instructions(preset:'/color-pop')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
