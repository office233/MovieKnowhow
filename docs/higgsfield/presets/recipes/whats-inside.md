# Whats Inside (`/whats-inside`)

- **output:** image
- **description:** The product's contents or ingredients.

Resolve with `get_preset_instructions(preset:'/whats-inside')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
