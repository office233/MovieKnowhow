# Vertical Hook (`/vertical-hook`)

- **output:** image
- **description:** A vertical visual with a strong hook.

Resolve with `get_preset_instructions(preset:'/vertical-hook')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
