# Travel Pack (`/travel-pack`)

- **output:** image
- **description:** The product in a travel setting.

Resolve with `get_preset_instructions(preset:'/travel-pack')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
