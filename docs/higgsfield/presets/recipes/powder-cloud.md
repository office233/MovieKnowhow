# Powder Cloud (`/powder-cloud`)

- **output:** image
- **description:** A cloud of colored powder.

Resolve with `get_preset_instructions(preset:'/powder-cloud')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
