# Shadow Motion (`/shadow-motion`)

- **output:** video
- **description:** Moving graphic shadows.

Resolve with `get_preset_instructions(preset:'/shadow-motion')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
