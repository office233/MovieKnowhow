# Texture Track (`/texture-track`)

- **output:** video
- **description:** A camera move along the material's texture.

Resolve with `get_preset_instructions(preset:'/texture-track')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
