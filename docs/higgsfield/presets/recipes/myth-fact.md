# Myth Fact (`/myth-fact`)

- **output:** image
- **description:** A myth paired with the actual fact.

Resolve with `get_preset_instructions(preset:'/myth-fact')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
