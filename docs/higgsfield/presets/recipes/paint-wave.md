# Paint Wave (`/paint-wave`)

- **output:** video
- **description:** A wave of colorful paint.

Resolve with `get_preset_instructions(preset:'/paint-wave')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
