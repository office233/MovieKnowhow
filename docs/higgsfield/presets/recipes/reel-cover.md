# Reel Cover (`/reel-cover`)

- **output:** image
- **description:** A static cover image for a Reel.

Resolve with `get_preset_instructions(preset:'/reel-cover')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
