# Three Reasons (`/three-reasons`)

- **output:** image
- **description:** Three reasons to choose the product.

Resolve with `get_preset_instructions(preset:'/three-reasons')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
