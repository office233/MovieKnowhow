# How It Works (`/how-it-works`)

- **output:** image
- **description:** An explanation of how the product works.

Resolve with `get_preset_instructions(preset:'/how-it-works')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
