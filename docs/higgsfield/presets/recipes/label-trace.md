# Label Trace (`/label-trace`)

- **output:** video
- **description:** A camera move across the logo and label text.

Resolve with `get_preset_instructions(preset:'/label-trace')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
