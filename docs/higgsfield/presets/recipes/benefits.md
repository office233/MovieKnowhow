# Benefits (`/benefits`)

- **output:** image
- **description:** The product's key benefits.

Resolve with `get_preset_instructions(preset:'/benefits')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
