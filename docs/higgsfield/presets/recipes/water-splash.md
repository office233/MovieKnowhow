# Water Splash (`/water-splash`)

- **output:** image
- **description:** Water forming a crown around the product.

Resolve with `get_preset_instructions(preset:'/water-splash')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
