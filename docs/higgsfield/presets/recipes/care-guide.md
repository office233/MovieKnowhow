# Care Guide (`/care-guide`)

- **output:** image
- **description:** Product care instructions.

Resolve with `get_preset_instructions(preset:'/care-guide')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
