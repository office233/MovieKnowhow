# Crane Reveal (`/crane-reveal`)

- **output:** video
- **description:** A vertical camera move revealing the product.

Resolve with `get_preset_instructions(preset:'/crane-reveal')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
