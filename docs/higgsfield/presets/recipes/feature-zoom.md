# Feature Zoom (`/feature-zoom`)

- **output:** image
- **description:** A demonstration of the product's key feature.

Resolve with `get_preset_instructions(preset:'/feature-zoom')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
