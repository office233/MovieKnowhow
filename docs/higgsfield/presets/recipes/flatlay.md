# Flatlay (`/flatlay`)

- **output:** image
- **description:** A neatly arranged overhead composition.

Resolve with `get_preset_instructions(preset:'/flatlay')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
