# Pull Back (`/pull-back`)

- **output:** video
- **description:** A camera pullback revealing the scene.

Resolve with `get_preset_instructions(preset:'/pull-back')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
