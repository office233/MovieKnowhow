# Usage Guide (`/usage-guide`)

- **output:** image
- **description:** Ways to use the product.

Resolve with `get_preset_instructions(preset:'/usage-guide')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
