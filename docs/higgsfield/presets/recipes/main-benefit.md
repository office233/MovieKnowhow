# Main Benefit (`/main-benefit`)

- **output:** image
- **description:** The product's single most important benefit.

Resolve with `get_preset_instructions(preset:'/main-benefit')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
