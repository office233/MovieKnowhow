# Angles (`/angles`)

- **output:** image
- **description:** Multiple product angles in one frame.

Resolve with `get_preset_instructions(preset:'/angles')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
