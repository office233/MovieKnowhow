# Fabric Wave (`/fabric-wave`)

- **output:** image
- **description:** Fabric flowing around the product.

Resolve with `get_preset_instructions(preset:'/fabric-wave')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
