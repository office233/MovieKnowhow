# On Desk (`/on-desk`)

- **output:** image
- **description:** The product on a work desk.

Resolve with `get_preset_instructions(preset:'/on-desk')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
