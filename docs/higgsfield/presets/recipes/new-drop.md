# New Drop (`/new-drop`)

- **output:** image
- **description:** A new product announcement.

Resolve with `get_preset_instructions(preset:'/new-drop')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
