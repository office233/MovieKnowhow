# Product Match (`/product-match`)

- **output:** image
- **description:** A guide to choosing the right product.

Resolve with `get_preset_instructions(preset:'/product-match')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
