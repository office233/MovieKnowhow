# Product Spin (`/product-spin`)

- **output:** video
- **description:** A clean 360-degree product rotation.

Resolve with `get_preset_instructions(preset:'/product-spin')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
