# Floating Roll (`/floating-roll`)

- **output:** video
- **description:** Slow product rotation in zero gravity.

Resolve with `get_preset_instructions(preset:'/floating-roll')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
