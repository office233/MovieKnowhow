# Sunrise Pass (`/sunrise-pass`)

- **output:** video
- **description:** A transition from darkness to warm light.

Resolve with `get_preset_instructions(preset:'/sunrise-pass')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
