# Inside Pack (`/inside-pack`)

- **output:** image
- **description:** The internal structure of the packaging.

Resolve with `get_preset_instructions(preset:'/inside-pack')` for the full recipe (prompt template, model, inputs). Full recipe text not captured — connector disconnected; see ../../open-questions.md.
